extends layout

block content
  .landing-wrapper
    h1.landing-title SalonEase
    p.landing-subtitle Salon appointment management
    .landing-actions
      a.btn-landing(href="/auth/login") Sign in
      a.btn-landing-outline(href="/auth/register") Register