extends layout

block content
  .landing-hero
    .landing-content
      h1.landing-title SalonEase
      p.landing-subtitle Book your perfect salon experience, anytime, anywhere
      .landing-actions
        a.btn-landing(href="/auth/login") Sign in
        a.btn-landing-outline(href="/auth/register") Create account
